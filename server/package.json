{
  "name": "tts-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Express API for the Text-to-Speech platform (Phase 2: POST /api/tts validates; synthesis arrives in Phase 3)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@echogarden/espeak-ng-emscripten": "^0.3.5",
    "express": "^5.1.0",
    "express-rate-limit": "^8.7.0",
    "lamejs": "^1.2.1"
  },
  "devDependencies": {
    "supertest": "^7.1.0",
    "vitest": "^3.2.0"
  }
}
